/** Max decoded image size for UI screenshot uploads (vision preflight). */
export const REFERENCE_IMAGE_MAX_BYTES = 350_000;

/** Max base64 string length (roughly 1.33× bytes + padding). */
export const REFERENCE_IMAGE_BASE64_MAX_LENGTH = 550_000;
